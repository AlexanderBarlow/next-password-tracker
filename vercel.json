{
  "builds": [
    { "src": "seeds/**", "use": "@now/node-server" },
    { "src": "src/**/*", "use": "@now/next" }
  ],
  "routes": [
    { "src": "/seeds/(.*)", "dest": "/seeds/$1" },
    { "src": "/(.*)", "dest": "/$1" }
  ]
}
